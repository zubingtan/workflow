diff --git a/dist/esm/index.js b/dist/esm/index.js
index 51c0433fbae8ebd8ef3af2a6ec26be97b07216f6..6e394a0ffd34137e6bf66468ac065e29a998d6e5 100644
--- a/dist/esm/index.js
+++ b/dist/esm/index.js
@@ -1675,9 +1675,6 @@ var startEndNode = (schema) => {
   if (startNodes.length > 1) {
     throw new Error("Workflow schema must have only one start node");
   }
-  if (endNodes.length > 1) {
-    throw new Error("Workflow schema must have only one end node");
-  }
   schema.nodes.forEach((node) => {
     if (node.blocks) {
       blockStartEndNode({
@@ -1884,6 +1881,7 @@ var WorkflowRuntimeTask = class _WorkflowRuntimeTask {
     this.processing = params.processing;
   }
   cancel() {
+    this.context.abortController.abort();
     this.context.statusCenter.workflow.cancel();
     const cancelNodeIDs = this.context.statusCenter.getStatusNodeIDs(WorkflowStatus.Processing);
     cancelNodeIDs.forEach((nodeID) => {
@@ -2843,6 +2841,7 @@ var WorkflowRuntimeContext = class _WorkflowRuntimeContext {
   constructor(data) {
     this.subContexts = [];
     this.id = uuid();
+    this.abortController = data.abortController ?? new AbortController();
     this.cache = data.cache;
     this.document = data.document;
     this.variableStore = data.variableStore;
@@ -2894,6 +2893,7 @@ var WorkflowRuntimeContext = class _WorkflowRuntimeContext {
       messageCenter: this.messageCenter,
       reporter: this.reporter,
       variableStore,
+      abortController: this.abortController,
       state
     };
     const subContext = new _WorkflowRuntimeContext(contextData);
@@ -2977,6 +2977,7 @@ var WorkflowRuntimeEngine = class {
         node,
         inputs,
         runtime: context,
+        signal: context.abortController.signal,
         container: WorkflowRuntimeContainer.instance,
         snapshot
       });
@@ -3246,12 +3247,18 @@ var WorkflowRuntimeAPIs = {
   [FlowGramAPIName.TaskCancel]: TaskCancelAPI,
   [FlowGramAPIName.TaskValidate]: TaskValidateAPI
 };
+function registerNodeExecutor(executor) {
+  var container = WorkflowRuntimeContainer.instance;
+  var executorInstance = container.get(IExecutor);
+  executorInstance.register(executor);
+}
 export {
   TaskCancelAPI,
   TaskReportAPI,
   TaskResultAPI,
   TaskRunAPI,
   TaskValidateAPI,
-  WorkflowRuntimeAPIs
+  WorkflowRuntimeAPIs,
+  registerNodeExecutor
 };
 //# sourceMappingURL=index.js.map
\ No newline at end of file
diff --git a/dist/index.d.cts b/dist/index.d.cts
index 41b0cc498a8c1e21e79c88d799f44af326ae6b85..c9789d11c27789a73ef8ceccec831150a14fdfb1 100644
--- a/dist/index.d.cts
+++ b/dist/index.d.cts
@@ -201,4 +201,6 @@ declare const TaskCancelAPI: (input: TaskCancelInput) => Promise<TaskCancelOutpu
 
 declare const WorkflowRuntimeAPIs: Record<FlowGramAPIName, (i: any) => any>;
 
-export { TaskCancelAPI, TaskReportAPI, TaskResultAPI, TaskRunAPI, TaskValidateAPI, WorkflowRuntimeAPIs };
+declare function registerNodeExecutor(executor: { type: string; execute: (context: any) => Promise<{ outputs: any; branch?: string }> }): void;
+
+export { TaskCancelAPI, TaskReportAPI, TaskResultAPI, TaskRunAPI, TaskValidateAPI, WorkflowRuntimeAPIs, registerNodeExecutor };
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 41b0cc498a8c1e21e79c88d799f44af326ae6b85..c9789d11c27789a73ef8ceccec831150a14fdfb1 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -201,4 +201,6 @@ declare const TaskCancelAPI: (input: TaskCancelInput) => Promise<TaskCancelOutpu
 
 declare const WorkflowRuntimeAPIs: Record<FlowGramAPIName, (i: any) => any>;
 
-export { TaskCancelAPI, TaskReportAPI, TaskResultAPI, TaskRunAPI, TaskValidateAPI, WorkflowRuntimeAPIs };
+declare function registerNodeExecutor(executor: { type: string; execute: (context: any) => Promise<{ outputs: any; branch?: string }> }): void;
+
+export { TaskCancelAPI, TaskReportAPI, TaskResultAPI, TaskRunAPI, TaskValidateAPI, WorkflowRuntimeAPIs, registerNodeExecutor };
diff --git a/dist/index.js b/dist/index.js
index 3c8b340c49028c61612954bd5cfc836a8a8cd21e..59f3bb4b7356a54e9ccaaa6cd6a0b4b7e2fbaa59 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1716,9 +1716,6 @@ var startEndNode = (schema) => {
   if (startNodes.length > 1) {
     throw new Error("Workflow schema must have only one start node");
   }
-  if (endNodes.length > 1) {
-    throw new Error("Workflow schema must have only one end node");
-  }
   schema.nodes.forEach((node) => {
     if (node.blocks) {
       blockStartEndNode({
@@ -1925,6 +1922,7 @@ var WorkflowRuntimeTask = class _WorkflowRuntimeTask {
     this.processing = params.processing;
   }
   cancel() {
+    this.context.abortController.abort();
     this.context.statusCenter.workflow.cancel();
     const cancelNodeIDs = this.context.statusCenter.getStatusNodeIDs(WorkflowStatus.Processing);
     cancelNodeIDs.forEach((nodeID) => {
@@ -2884,6 +2882,7 @@ var WorkflowRuntimeContext = class _WorkflowRuntimeContext {
   constructor(data) {
     this.subContexts = [];
     this.id = uuid();
+    this.abortController = data.abortController ?? new AbortController();
     this.cache = data.cache;
     this.document = data.document;
     this.variableStore = data.variableStore;
@@ -2935,6 +2934,7 @@ var WorkflowRuntimeContext = class _WorkflowRuntimeContext {
       messageCenter: this.messageCenter,
       reporter: this.reporter,
       variableStore,
+      abortController: this.abortController,
       state
     };
     const subContext = new _WorkflowRuntimeContext(contextData);
@@ -3018,6 +3018,7 @@ var WorkflowRuntimeEngine = class {
         node,
         inputs,
         runtime: context,
+        signal: context.abortController.signal,
         container: WorkflowRuntimeContainer.instance,
         snapshot
       });
@@ -3287,6 +3288,11 @@ var WorkflowRuntimeAPIs = {
   [FlowGramAPIName.TaskCancel]: TaskCancelAPI,
   [FlowGramAPIName.TaskValidate]: TaskValidateAPI
 };
+function registerNodeExecutor(executor) {
+  var container = WorkflowRuntimeContainer.instance;
+  var executorInstance = container.get(IExecutor);
+  executorInstance.register(executor);
+}
 // Annotate the CommonJS export names for ESM import in node:
 0 && (module.exports = {
   TaskCancelAPI,
@@ -3294,6 +3300,7 @@ var WorkflowRuntimeAPIs = {
   TaskResultAPI,
   TaskRunAPI,
   TaskValidateAPI,
-  WorkflowRuntimeAPIs
+  WorkflowRuntimeAPIs,
+  registerNodeExecutor
 });
 //# sourceMappingURL=index.js.map
