name: PR Gate

on:
  pull_request:

permissions:
  contents: read

jobs:
  m0-acceptance:
    name: M0 acceptance
    uses: ./.github/workflows/m0-acceptance.yml
    with:
      git_sha: ${{ github.event.pull_request.head.sha }}
      run_label: pr
