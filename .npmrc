# Enforce the Node version declared in package.json `engines` (node ^22.23.1).
# Without this, pnpm only warns on a Node mismatch; with it, `pnpm install`
# fails fast. This prevents the better-sqlite3 native-binding ABI drift that
# occurs when installing under a different Node major version.
engine-strict=true
